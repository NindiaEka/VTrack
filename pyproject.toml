[project]
name = "vtrack"
version = "2.0.0"
description = "Video inference and analytics pipeline with YOLO"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
  "ultralytics",
  "opencv-python",
  "pyyaml",
  "lap>=0.5.12",
  "numpy",
  "pytest",
]

[tool.uv]
package = false
